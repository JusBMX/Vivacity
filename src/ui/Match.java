package ui;

import graphics.Screen;

public class Match extends UI {

	@Override
	public void tick() {

	}

	@Override
	public void render(Screen screen) {
		//screen.renderText("Force: " + Integer.toString(force), 10, 16, false);
		// screen.renderText("Time left: " + Integer.toString(timer.timeLeft()),
		// 250, 16, false);
	}

}
